{
  "name": "mspmo",
  "version": "0.1.0",
  "private": true,
  "description": "MSPMO — MSP Project Management Office Platform",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.12.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
